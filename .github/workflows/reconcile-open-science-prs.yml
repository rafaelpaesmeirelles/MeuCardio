name: Reconcile open scientific PR corpus

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'A reconciliação V4 já foi certificada e gravada em science/reconcile-open-pr-corpus-20260904.'
          echo 'Novas rodadas só devem ser iniciadas manualmente para evitar reruns redundantes.'
