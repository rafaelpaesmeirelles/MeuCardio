fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.51
psycopg[binary]==3.2.3
pydantic==2.10.4
pydantic-settings==2.7.0
PyJWT==2.13.0
# Implementação bcrypt direta; evita dependência que importa o módulo legado `crypt`.
bcrypt==4.0.1
python-multipart==0.0.32
redis==5.2.1
markdown-it-py==3.0.0
python-frontmatter==1.1.0
openai==1.59.6
anthropic==0.120.2
pgvector==0.3.6
tenacity==9.1.4
alembic==1.14.0
httpx==0.28.1
stripe==15.3.1
cryptography==50.0.0
tzdata==2024.2
# Decodificação estrutural de JPEG/PNG/WEBP nos uploads. Dependência direta,
# em vez de depender acidentalmente da versão transitiva do ReportLab.
pillow==12.3.0
# Rasterização de PDFs clínicos para remover metadados, anotações e conteúdo oculto.
pymupdf==1.26.7
# Geração de PDF de todos os documentos do sistema (receita, atestado, laudo).
# Mantida na linha 4.x: 4.4.10 declara suporte a Python 3.14 e elimina o uso
# legado de ast.NameConstant, sem assumir as mudanças incompatíveis da versão 5.
reportlab==4.4.10
# Assinatura digital PAdES a partir de certificado A1 local (.pfx/.p12) do
# próprio médico (Trabalho 7, 06/08/2026) — biblioteca padrão de fato em
# Python pra assinatura de PDF ICP-Brasil-compatível, usa `cryptography`
# (já dependência) como backend criptográfico.
pyhanko==0.36.2
# QR da URL pública de validação desenhado pelo QRStampStyle do pyHanko.
# O pyHanko trata qrcode como dependência opcional, portanto pinamos
# explicitamente para que produção nunca dependa de instalação transitiva.
qrcode==8.2
# Templates dos e-mails transacionais (02/08/2026) — HTML + texto puro
# renderizados a partir de backend/app/templates/emails/.
jinja2==3.1.6
# Modo Apresentação: exportação em PowerPoint editável (07/08/2026), a par do
# PDF já existente — pedido do Rafael para o assinante poder modificar os
# slides antes de projetar. Formato .pptx nativo (não é PDF disfarçado).
python-pptx==1.0.2
# Exportação universal em Word editável. O DOCX é OOXML nativo e preserva
# títulos, listas, referências e proveniência para edição pelo assinante.
python-docx==1.2.0
