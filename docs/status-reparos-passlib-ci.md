# Certificação pendente — bcrypt direto

PR: a abrir.

A implementação já está pronta e os vetores históricos `$2a$`, `$2b$` e `$2y$` foram validados antes da CI. Este registro será atualizado com o número do PR, execução da CI, revisão e merge.
