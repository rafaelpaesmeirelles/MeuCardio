Temporary operational marker for the two-stage production recovery. No application behavior. Stage 1 temporarily restores the recovery-base migration tree and uses a frontend-only deploy.sh to bring the existing recovery backend and Caddy back online. Stage 2 immediately restores the certified current migration tree and full deploy.sh, then publishes the current main normally. This file may remain as an audit note.